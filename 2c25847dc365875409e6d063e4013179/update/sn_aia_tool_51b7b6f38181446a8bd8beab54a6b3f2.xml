<?xml version="1.0" encoding="UTF-8"?><record_update table="sn_aia_tool">
  <sn_aia_tool action="INSERT_OR_UPDATE">
    <sys_id>51b7b6f38181446a8bd8beab54a6b3f2</sys_id>
    <sys_scope display_value="x_snc_medwc_auto_0">2c25847dc365875409e6d063e4013179</sys_scope>
    <sys_update_name>sn_aia_tool_51b7b6f38181446a8bd8beab54a6b3f2</sys_update_name>
    <active>true</active>
    <description>Searches the Medtric Warranty Policies knowledge base for relevant coverage policy articles based on keywords from the claim reason.</description>
    <input_schema>[{"name":"search_terms","description":"Keywords to search for in KB articles (e.g., 'physical damage drop', 'software firmware', 'component failure')","mandatory":true}]</input_schema>
    <name>Search Warranty Knowledge Base</name>
    <post_processing_script/>
    <record_type>custom</record_type>
    <script><![CDATA[(function(inputs) {
    var results = [];
    var keywords = inputs.search_terms.toLowerCase().split(' ');
    var gr = new GlideRecord('kb_knowledge');
    gr.addQuery('kb_knowledge_base.title', 'Medtric Warranty Policies');
    gr.addQuery('workflow_state', 'published');
    gr.addQuery('active', true);
    gr.query();
    while (gr.next()) {
        var text = (gr.getValue('text') || '').toLowerCase();
        var title = (gr.getValue('short_description') || '').toLowerCase();
        var matchCount = 0;
        for (var i = 0; i < keywords.length; i++) {
            if (keywords[i].length > 2 && (text.indexOf(keywords[i]) >= 0 || title.indexOf(keywords[i]) >= 0)) {
                matchCount++;
            }
        }
        if (matchCount > 0) {
            results.push({
                title: gr.getValue('short_description'),
                number: gr.getValue('number'),
                text: gr.getValue('text').replace(/<[^>]*>/g, '').substring(0, 800),
                relevance: matchCount
            });
        }
    }
    results.sort(function(a, b) { return b.relevance - a.relevance; });
    if (results.length === 0) {
        return JSON.stringify({ status: 'no_results', message: 'No relevant KB articles found for: ' + inputs.search_terms });
    }
    return JSON.stringify({ status: 'success', articles: results.slice(0, 3) });
})(inputs);]]></script>
    <sys_domain>global</sys_domain>
    <sys_overrides/>
    <target_document/>
    <target_document_table>sn_aia_tool</target_document_table>
    <type>script</type>
  </sn_aia_tool>
</record_update>
